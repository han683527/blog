# 半笺云纸 · 用户端

Vue 3 + Vite + Element Plus。开发时通过 Vite 代理访问本地后端 `http://localhost:8080`。

```bash
npm install
npm run dev
```

浏览器打开 http://localhost:5173/

生产构建：`npm run build`，产物交给 `deploy/nginx/dist/`（该目录不入库）。
